@page
@model LogoutModel

@{
    ViewData["Title"] = "Log Out";
    Layout = "Shared/_Layout";
}

<div class="d-flex h-100 flex-column flex-grow-1 justify-content-center align-items-center">
    <div class="bg-light shadow-lg d-flex flex-column p-5">
        <div class=" text-center d-flex flex-column align-items-center m-5">
            <h1 class="mb-3">Logged out</h1>
            <a class="btn btn-success btn-lg" asp-page="/Index">Go Home</a>
        </div>
    </div>

</div>